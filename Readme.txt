running train_regression files will produce a graph of loss vs step numbers and also store the model parameters in a JSON file 

running eval_regression files will load the model parameters from the respective json file and print the MSE on the console


